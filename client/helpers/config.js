/*
Just need username and email (and password of course...) to sign up (i don't care about lastname and firstname because I'm against PRISM)
*/
Accounts.ui.config({
  passwordSignupFields: 'USERNAME_AND_EMAIL'
});
